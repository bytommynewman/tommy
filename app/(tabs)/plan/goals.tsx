import React from 'react';
import { PlaceholderScreen } from '../../../components/ui/PlaceholderScreen';

export default function GoalsScreen() {
  return (
    <PlaceholderScreen
      title="Goals"
      note="Coming in M5 — categorized goals (career, business, fitness, relationships, finance, personal brand, family, US move) with milestone checklists."
    />
  );
}
