import React from 'react';
import { PlaceholderScreen } from '../../../components/ui/PlaceholderScreen';

export default function CalendarScreen() {
  return (
    <PlaceholderScreen
      title="Calendar"
      note="Coming in M4 — a month/day view with events for school, work, content shoots, and social plans."
    />
  );
}
